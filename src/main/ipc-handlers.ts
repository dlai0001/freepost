/**
 * Main-process IPC handlers. Filled in by the integration phase; modules under
 * src/core are pure and unit-tested, this file wires them to the renderer.
 */
export function registerIpcHandlers(): void {
  // Wired during integration (task: Integrate, test, wire IPC, CI).
}
