node_modules/
dist/
out/
*.log
.DS_Store
.freepost/
*.local.env.json
coverage/
.claude/

# Scratch dirs created by the MCP tests (a spawned stdio server script must
# live inside the project so node can resolve @modelcontextprotocol/sdk).
.tmp-*/
